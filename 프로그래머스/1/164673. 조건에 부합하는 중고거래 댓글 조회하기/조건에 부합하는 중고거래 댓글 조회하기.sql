-- SELECT A.TITLE
--      , A.BOARD_ID
--      , B.REPLY_ID
--      , B.WRITER_ID
--      , B.CONTENTS
--      , TO_CHAR(B.CREATED_DATE, 'YYYY-MM-DD') AS CREATED_DATE
--   FROM USED_GOODS_BOARD A JOIN USED_GOODS_REPLY B 
--     ON (A.BOARD_ID = B.BOARD_ID)
--  WHERE TO_CHAR(A.CREATED_DATE, 'YYYYMM')='202210'
--  ORDER BY B.CREATED_DATE ASC, A.TITLE ASC;


SELECT B.TITLE, B.BOARD_ID, R.REPLY_ID, R.WRITER_ID, R.CONTENTS, to_char(R.CREATED_DATE, 'YYYY-MM-DD')
FROM USED_GOODS_BOARD B
JOIN USED_GOODS_REPLY R
    ON B.BOARD_ID = R.BOARD_ID
WHERE B.CREATED_DATE BETWEEN TO_DATE('20221001','YYYYMMDD') AND TO_DATE('20221031','YYYYMMDD')
ORDER BY R.CREATED_DATE, B.TITLE;